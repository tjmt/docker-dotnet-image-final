# docker-compose -f docker-compose.build.yml build --no-cache
# docker-compose -f docker-compose.build.yml push

version: '3.5'
services:
  dotnetcore-builder:
    image: nexusdocker.tjmt.jus.br/dsa/dotnetcore:2.2-runtime
    build: 
      context: .
      dockerfile: Dockerfile   